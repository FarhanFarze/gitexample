function averagearray(arr){
  let sum = 0;
  for (let i = 0; i < arr.length; i++)
  {
    sum = sum + arr[i];
  }
  let averagesum = sum / arr.length;
  return average;
}
let orginalArray = [1,2,3,4,5];
console.log(averageArray(orginalArray));